
/* Material and Tool */

/* Tools */
tool(shovel).
tool(fishing_rod).

/* crop */
	/* Spring */
	crop(turnip).
	crop(potato).
	crop(cucumber).
	crop(cabbage).
	/*Summer*/
	crop(tomato).
	crop(corn).
	crop(onion).
	crop(pineapple).
	/* Fall */
	crop(carrot).
	crop(eggplant).
	crop(sweet_potato).
	crop(green_paper).
	/* winter */
	/*no crop */

/* Animal product */
isProduct(egg).
isProduct(milk).
isProduct(wool).

/* fish */
isFish(small_fish).
isFish(medium_fish).
isFish(large_fish).

/* price */
price(turnip,60).
price(potato,80).
price(cucumber,60).
price(cabbage,250).
price(tomato,60).
price(corn.100.
price(onion.80.
price(pineapple,500).
price(carrot,120).
price(eggplant,80).
price(sweet_potato,120).
price(green_paper,40).

price(egg,50).
price(milk,200).
price(wool,500).

price(small_fish, 50).
price(medium_fish, 120).
price(large_fish. 200).

/* crop section */
seasonCrop(turnip,spring).
seasonCrop(potato,spring).
seasonCrop(cucumber,spring).
seasonCrop(cabbage,spring).
seasonCrop(tomato,summer).
seasonCrop(corn,summer).
seasonCrop(onion,summer).
seasonCrop(pineapple,summer).
seasonCrop(carrot,fall).
seasonCrop(eggplant,fall).
seasonCrop(sweet_potato,fall).
seasonCrop(green_paper,fall).

/* Animal product section */
isAnimal(chicken).
isanimal(sheep).
isAnimal(cow).
product(chicken,egg).
product(cow,milk).
product(sheep,wool).

/* Fish section */

